# Netlify 部署配置 —— 树成林 AI 榜
# 本项目为纯静态站点，无需构建步骤。

[build]
  publish = "."
  command = ""

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
